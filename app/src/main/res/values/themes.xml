<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Todo" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/green_500</item>
        <item name="colorPrimaryVariant">@color/green_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/orange_200</item>
        <item name="colorSecondaryVariant">@color/orange_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Todo.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="toolbar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.DayNight.ActionBar</item>
        <item name="android:textColorPrimary">?attr/colorOnPrimary</item>
        <item name="android:textColorSecondary">?attr/colorOnPrimary</item>
        <item name="tint">?attr/colorOnPrimary</item>
        <item name="iconTint">?attr/colorOnPrimary</item>
        <item name="android:background">?attr/colorPrimary</item>
    </style>


    <style name="welcome_text">
        <item name="android:textSize">@dimen/size_huge_text</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="welcome_subtext" parent="welcome_text">
        <item name="android:textSize">@dimen/size_large_text</item>
    </style>

    <style name="instruction_text">
        <!--        <item name="android:textSize">@dimen/size_large_text</item>-->
        <item name="android:gravity">center</item>
    </style>

    <style name="transparent_button">
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
        <item name="android:textColor">?attr/colorPrimary</item>
    </style>

    <style name="transparent_button.large">
        <item name="android:padding">@dimen/padding_large_button</item>
        <item name="android:textSize">@dimen/size_large_text</item>
    </style>

    <style name="text_header_navigation_drawer">
        <item name="android:gravity">center</item>
        <item name="android:textSize">@dimen/size_huge_text</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="text_todo_list_title">
        <item name="android:textSize">@dimen/size_large_text</item>
        <item name="android:textColor">@color/text_highlight</item>
    </style>

    <style name="bottom_sheet_button" parent="Widget.MaterialComponents.Button.TextButton.Dialog" />
</resources>